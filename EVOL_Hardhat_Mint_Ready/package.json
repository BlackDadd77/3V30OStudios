{
  "name": "evol-hardhat-mint-ready",
  "version": "1.0.0",
  "description": "EVOL War Codex ENFT Minting Kit for Avalanche",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network avalanche",
    "deploy:fuji": "hardhat run scripts/deploy.js --network fuji",
    "mint": "hardhat run scripts/mint.js --network avalanche",
    "mint:fuji": "hardhat run scripts/mint.js --network fuji",
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "keywords": ["EVOL", "ENFT", "Avalanche", "NFT", "Hardhat"],
  "author": "EvolVerse",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
